[Unit]
Description=Aeternity node
Requires=network-online.target
After=network-online.target

[Service]
Type=forking
User={{ project_user }}
Group=users
ExecStart={{ project_root }}/bin/aeternity start
ExecStop={{ project_root }}/bin/aeternity stop
Restart=on-failure
# Systemd does not play well with user set limits with ulimit.
LimitNOFILE=24576

[Install]
WantedBy=multi-user.target
