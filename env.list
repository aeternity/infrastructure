# A role to request permissions for
CREDS_ROLE
# URL address of the Vault server
VAULT_ADDR
AE_VAULT_ADDR
# Vault token authentication
VAULT_AUTH_TOKEN
AE_VAULT_AUTH_TOKEN
# Vault GitHub authentication (personal access token).
VAULT_GITHUB_TOKEN
AE_VAULT_GITHUB_TOKEN
# Vault AppRole authentication
VAULT_ROLE_ID
VAULT_SECRET_ID
